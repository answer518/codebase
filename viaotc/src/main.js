import { AppContainer } from 'react-hot-loader';
import React from 'react';
import ReactDOM from 'react-dom';
import AppIndex from 'LAYOUTS/frame';

const root = document.getElementById('root');

ReactDOM.render(
  <AppContainer>
    <AppIndex />
  </AppContainer>,
root);